export const RULE_KEYS = [
  'no_edit',
  'max_len',
  'no_links',
  'no_explicit',
  'bio_block',
  'funny_suffix',
];

export const DEFAULT_RULES = {
  no_edit: true,
  max_len: true,
  no_links: true,
  no_explicit: true,
  bio_block: true,
  funny_suffix: true,
};

export const DEFAULT_LIMITS = {
  max_len: 300,
};
